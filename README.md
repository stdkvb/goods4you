# Googs4you | Открытая школа T1

### React + TypeScript + Vite

#### Installation

Requires [Node.js](https://nodejs.org/) v18.12 to run.
Install the dependencies and start the server.

```sh
pnpm install
pnpm run dev
pnpm run storybook
pnpm run test
```
