export interface catalogItem {
  id: number;
  name: string;
  price: number;
  imageUrl: string;
  count: number;
  variant: string;
}

export interface faqItem {
  id: number;
  question: string;
  answer: string;
}
